import { colors } from './colors'
import { radius } from './radius'
import { shadows } from './shadows'

export const themeTokens = {
  colors,
  radius,
  shadows,
}

export const cssVariables = {
  '--ui5-primary': colors.primaryMidnightBlue,
  '--ui5-accent': colors.accentIntelligentTeal,
  '--ui5-surface': colors.surfaceOffWhite,
  '--ui5-text': colors.textCharcoalGray,
}
